[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    integration: requires a real throwaway Postgres database (opt-in, excluded by default)
addopts = -q --strict-markers -m "not integration"
