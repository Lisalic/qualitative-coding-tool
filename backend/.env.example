# Example env file (do NOT commit secrets)

# Database Configuration (no secrets here)
AUTH_DATABASE_URL=postgresql://<user>:<password>@<host>:5432/<database>

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# Existing Configuration
DATABASE_URL=postgresql://<user>:<password>@<host>:5432/<database>
DATABASE_DIR=./data/databases
FILTERED_DATABASE_DIR=./data/filtered_databases
SECRET_KEY=your-secret-key-here

# Standard Postgres environment variables (useful for tools/extensions)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=qualitative_coding_tool
