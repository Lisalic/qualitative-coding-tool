import React from "react";

// Protection intentionally disabled. This component now always allows access.
const ProtectedRoute = ({ children }) => {
  return children;
};

export default ProtectedRoute;
